datasource db {
  provider = "sqlite"
  url      = "file:./data.db"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id String @id
  username String @unique
  password String
  money Int @default(0)
  admin Boolean @default(false)
  registertime String
}